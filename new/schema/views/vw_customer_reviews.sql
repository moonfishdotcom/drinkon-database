# SP - I've moved this view definition to vw_customers_with_titles.sql to get around the ordering dependency
# (this view is used by the _with_titles view but comes before it alphabetically in the create database script)